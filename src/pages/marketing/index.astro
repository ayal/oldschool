---
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../../consts';
import { getCollection } from 'astro:content';
import Lesson from '../../layouts/Lesson.astro';

const lessons = (await getCollection('lesson'))
// find marketing lesson:
const marketingLesson = lessons.find(lesson => lesson.data.title === 'Marketing');
const title = marketingLesson?.data.title
const videos = marketingLesson?.data.videos

---

<Lesson {...marketingLesson?.data} />
